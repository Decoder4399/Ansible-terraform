install in control node.

sudo apt update
sudo apt install ansible -y
ansible --version

-----------------------------------------------------------
sudo mkdir -p /etc/ansible
sudo vim /etc/ansible/ansible.cfg

[defaults]
inventory = /etc/ansible/hosts
host_key_checking = False


-----------------------------------------------------------
sudo vim /etc/ansible/hosts

[servers]
Worker-CentOS ansible_host=13.239.244.128 ansible_user=ec2-user
Worker-ubuntu ansible_host=3.106.52.245 ansible_user=ubuntu

[servers:vars]
ansible_python_interpreter=/usr/bin/python3
ansible_ssh_private_key_file=/home/ubuntu/keys/terra-key-ansible
ansible_host_key_checking=false

-----------------------------------------------------------

mkdir keys
cd keys
vim /home/ubuntu/keys/terra-key-ansible
sudo chmod 400 /home/ubuntu/keys/terra-key-ansible

----------------------------------------------------------------

ansible servers -m ping








